# Hermes project context

This repository uses `AGENTS.md` as the canonical, portable project memory and working guidance. Read it before modifying code; it is deliberately shared with Codex, Claude Code, OpenCode, and other agents rather than stored in Hermes’ machine-local memory database.

Hermes-specific behavior:

- Preserve unrelated working-tree changes and inspect `git status` before edits.
- Treat `AGENTS.md` as the source of truth for architecture, conventions, known gaps, and verification cautions.
- For test-plugin remote deployment, also read `MCCodeCampLibraryTestPlugin/DEPLOYMENT.md`. New machines should use the cross-platform ignored `deploy-local.py` template, not copy the legacy `.sh` deployment into native Windows.
- Use Hermes’ normal project-context loading; do not rely on `C:\Users\simon\AppData\Local\hermes` or a profile-specific memory store for project facts.
- Do not edit `.agentbridge/` or Hermes databases directly.
- Before claiming completion, exercise the requested code path and report actual tool output.

The older `.claude/PROJECT_MEMORY.md` was removed after the portable files were created; it was ignored by Git and was not canonical.
